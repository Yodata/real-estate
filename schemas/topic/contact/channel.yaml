# @format

subscribe:
  message:
    oneOf:
      - $ref: contact.add.yaml
      - $ref: contact.comment.yaml
      - $ref: contact.delete.yaml
      - $ref: contact.update.yaml
      - $ref: contact.plandelete.yaml
      - $ref: contact.planupdate.yaml
      - $ref: contact.collectioncreate.yaml
      - $ref: contact.collectiondelete.yaml
      - $ref: contact.collectionmemberadd.yaml
      - $ref: contact.collectionmemberremove.yaml
        # - $ref: contact.activity.yaml 
