allOf:
- properties:
    question:
      type: object
      example:
        type: Question
        text: What is the meaning of life?
    recipient:
      type: string
      example: https://{user}.example.com/profile/card#me
- $ref: Action.yaml
- properties:
    type:
      enum:
        - AskAction
    
    
