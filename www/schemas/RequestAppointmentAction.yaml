allOf:
  - $ref: Action.yaml
  - x-range: RequestAppointmentAction
    type: object
    description: a user (agent) is  requesting an appointment with the recipient
    properties:
      type:
        type: string
        enum:
          - RequestAppointmentAction
      text:
        type: string
        description: request text
        example: Accusantium eos neque doloribus illo nobis.
      agent:
        $ref: agent.yaml
      recipient:
        $ref: recipient.yaml
      about:
        $ref: about.yaml
      