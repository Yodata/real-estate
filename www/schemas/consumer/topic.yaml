publish:
  headers:
    $ref: ../publish-headers.yaml
  payload:
    type: object
    description: the event payload
subscribe:
  oneOf:
    - $ref: consumer.subscribe.yaml
    - $ref: consumer.unsubscribe.yaml