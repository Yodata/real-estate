allOf:
  - $ref: Action.yaml
  - type: object
    properties:
      type:
        type: string
        enum:
          - AddAction
    example:
      type: AddAction
      agent: https;//user.example.com/profile/card#me
      object:
        id: https://user.example.com/profile/card
