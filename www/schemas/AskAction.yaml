allOf:
  - properties:
      agent:
        description: the one who asks
      recipient:
        description: the one who should answer
      question:
        example:
          type: Question
          text: What is the meaning of life?
  - $ref: Action.yaml
  - properties:
      type:
        enum:
          - AskAction
