type: object
required:
  - type
properties:
  type:
    description: Linked-Data (@type)
    type: string
  id:
    description: Linked-Data URI (@id)
    type: string
    format: uri
    example: https://example.com/collection/1
