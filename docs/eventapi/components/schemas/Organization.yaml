allOf:
  - $ref: Thing.yaml
  - description:
      Organization is the base class for groups, companies, affiliates,
      franchisors, RealEstateAgent and teams.
    type: object
    properties:
      type:
        type: string
        enum:
          - Organization
      subOrganization:
        type: array
        items:
          description: reference to a related Organization
          type: string
        example:
          - http://office.example.com/profile/card#me
      parentOrganization:
        type: array
        items:
          description: Inverse of subOrganization
          type: string
        example:
          - http://example.com/profile/card#me
      contactPoint:
        type: array
        items:
          type: object
          x-kind: ContactPoint
          $ref: ContactPoint.yaml
