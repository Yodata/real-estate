x-range: Contact
x-sameas: https://schema.org/Person
title: Contact
allOf:
  
  - $ref: Person.yaml
  - description: |
      A contact record.

      sameAs: [schema:Person](https://schema.org/Person)

    properties:
      type:
        enum:
          - Contact

      leadSource:
        description: the Lead Provider
        type: object
        example:
          type: RealEstateOrganization
          id: https://{affiliate}.example.com/profile/card#me