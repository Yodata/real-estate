x-title: contact_added
x-scope: contact
summary: a contact was created
payload:
  allOf:
    - $ref: AddAction.yaml
    - type: object
      properties:
        topic:
          example:
            - contact
        object:
          $ref: Contact.yaml
