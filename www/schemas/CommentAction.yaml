description: The act of generating a comment about a subject.
allOf:
  - $ref: Action.yaml
  - type: object
    properties:
      type:
        type: string
        enum:
          - CommentAction
      object:
        $ref: Comment.yaml
