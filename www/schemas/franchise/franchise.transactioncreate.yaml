x-title: transaction.create
x-scope: realestate/franchise#
summary: fires when a new transaction is created
payload:
  allOf:
    - $ref: ../base.event.yaml
    - properties:
        topic:
          description: realestate/franchise#transactionreport
          enum:
            - realestate/franchise#transactionreport
        data:
          allOf:
            - $ref: ../CreateAction.yaml
            - properties:
                object:
                  $ref: ../RealEstateTransaction.yaml
