type: object
title: PropertyValue
description: a key/value pair, used for maintaining state outside of the current context
required:
  - type
  - propertyID
  - value
properties:
  propertyID:
    type: string
    example: UserID
  type:
    type: string
    enum:
      - PropertyValue
  value:
    type: string
    example: '@bobjones'
x-parser-schema-id: PropertyValue
$id: http://realestate.yodata.me/schema/PropertyValue.yaml

