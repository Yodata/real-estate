title: image
description: an ImageObject or URI reference to an image on the web.
type: array
x-range:
  - ImageObject
  - URI
items:
  oneOf:
    - type: object
      $ref: ImageObject.yaml
    - type: string
      format: uri
