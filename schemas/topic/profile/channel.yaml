# @format
# yaml-language-server: $schema=https://asyncapi.com/definitions/2.5.0/channelItem.json
description: profile events
subscribe:
  message:
    oneOf:
      - $ref: add.yaml
      - $ref: update.yaml
      - $ref: teamcreate.yaml
      - $ref: teamdelete.yaml
      - $ref: teammemberadd.yaml
      - $ref: teammemberremove.yaml
