publish:
  headers:
    $ref: ../publish-headers.yaml
  payload:
    type: object
    description: the event payload
subscribe:
  oneOf:
    - $ref: franchise.transactionreport.yaml
    - $ref: franchise.listingreport.yaml