x-title: lead#asssign
x-scope: realestate
summary: a lead was assigned
payload:
  allOf:
    - $ref: Event.yaml
    - properties:
        topic:
          enum:
            - realestate/lead#assign
        data:
          $ref: ../schemas/AssignAction.yaml
