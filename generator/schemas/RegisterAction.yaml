allOf:
  - $ref: Action.yaml
  - x-range: RegisterAction
    type: object
    description: the user (agent) has created a website account.
    properties:
      type:
        type: string
        enum:
          - RegisterAction
