type: object
title: PropertyValue
description: a key/value pair, used for maintaining state outside of the current context
required:
  - type
  - propertyID
  - value
properties:
  type:
    type: string
    enum:
      - PropertyValue
  propertyID:
    type: string
    example: UserID
  value:
    type: string
    example: '@bobjones'
