messageId: contact#comment
x-scope: realestate/contact#
summary: a comment was made by or associated with the item (about)
payload:
  allOf:
    - $ref: base.event.yaml
    - properties:
        topic:
          enum:
            - realestate/contact#comment
        data:
          $ref: Comment.yaml
