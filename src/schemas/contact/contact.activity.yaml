
messageId: contact#activity
x-scope: realestate/contact#
summary: an action was performed by the user/contact
payload:
  allOf:
    - $ref: ../base.event.yaml
    - properties:
        topic:
          enum:
            - realestate/contact#comment
        data:
          $ref: ../Comment.yaml
