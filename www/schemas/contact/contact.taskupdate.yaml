x-title: taskupdate
x-scope: realestate/contact#
summary: a task has been updated
payload:
  allOf:
    - $ref: ../base.event.yaml
    - properties:
        topic:
          enum:
            - realestate/contact#taskupdate
        data:
          allOf:
          - $ref: ../UpdateAction.yaml
          - properties:
              object:
                $ref: ../Task.yaml
                description: the task to be updated


    
