x-range: Contact
x-sameas: https://schema.org/Person
title: Contact
allOf:
  
  - $ref: Person.yaml
  - description: |
      A Contact

    properties:
      type:
        enum:
          - Contact

      address:
        type: array
        x-range: PostalAddress
        description: a mailing address for the item
        items:
          $ref: address.yaml

      leadSource:
        description: the Lead Provider
        example: https://{lead-provider}.example.com/profile/card#me

      keywords:
        type: array
        description: Keywords or tags assigned to the contact
        example:
        - Sphere of Influence
        - Past Customer
        items:
          type: string       
          