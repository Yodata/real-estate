x-title: lead#delete
x-scope: realestate
summary: a lead was deleted
payload:
  allOf:
    - $ref: Event.yaml
    - properties:
        topic:
          enum:
            - realestate/lead#delete
        data:
          $ref: ../schemas/DeleteAction.yaml
