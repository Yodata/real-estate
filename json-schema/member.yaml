description: describes members and roles for this organization.
type: array
x-range:
  - OrganizationRole
  - URI
items:
  anyOf:
    - $ref: OrganizationRole.yaml
    - type: string
      format: uri
