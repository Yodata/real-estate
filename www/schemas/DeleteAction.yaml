allOf:
  - $ref: Action.yaml
  - type: object
    properties:
      type:
        type: string
        enum:
          - DeleteAction
      agent:
        example: https://{ideleteyou}.example.com/profile/card#me
      instrument:
        example: https://{delete-o-matic2K}.example.com/profile/card#me
      object:
        description: the deleted item
      targetCollection:
        type: object
        description: the collection from which the object was removed
