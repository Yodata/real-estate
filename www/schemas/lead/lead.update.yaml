x-title: update
x-scope: realestate/lead#
summary: a crm lead was updated
payload:
  allOf:
    - $ref: ../base.event.yaml
    - properties:
        topic:
          enum:
            - realestate/lead#update
        data:
          type: object
          properties:
            type:
              type: string
              enum:
                - UpdateAction
              example: UpdateAction
            object:
              $ref: ../Lead.yaml
              example:
                type: Lead
