# @format

publish:
  headers:
    $ref: ../publish-headers.yaml
  payload:
    type: object
    description: the event payload
subscribe:
  oneOf:
    - $ref: update.yaml
    - $ref: listing.delete.yaml
