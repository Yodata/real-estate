x-title: contact#update
x-scope: realestate
summary: a crm contact was updated
payload:
  allOf:
    - $ref: Event.yaml
    - properties:
        topic:
          enum:
            - realestate/contact#update
        data:
          allOf:
            - $ref: ../schemas/UpdateAction.yaml
            - properties:
                object:
                  example:
                    type: Contact
                    name: the updated contact
                targetCollection:
                  $ref: ../schemas/Collection.yaml
