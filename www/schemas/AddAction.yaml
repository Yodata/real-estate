allOf:
  - $ref: Action.yaml
  - x-range: AddAction
    title: AddAction
    description: data (object) is added by user (agent), optionally to the targetCollection
    properties:
      type:
        description: AddAction
        enum:
          - AddAction
      agent:
        type: object
        description:  user who added the item
      object:
        type: object
        description: data which was added
      targetCollection:
        type: object
        description: collection receiving the data (object)
      