allOf:
  - $ref: Action.yaml
  - properties:
      type:
        enum:
          - AskAction
      agent:
        description: the one who asks
      recipient:
        description: the one who should answer
      question:
        example:
          type: Question
          text: What is the meaning of life?
      instrument:
        type: object
        x-range:
        - RealEstateWebsite
        - MobileApplication
        example:
          type: RealEstateWebsite
          name: Use id for a base domain or url for a deep link
          id: http://example.com
          url: http://example.com/property/detail/page/?xxx=333
