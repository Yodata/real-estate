x-title: lead#update
x-scope: realestate
summary: a crm lead was updated
payload:
  allOf:
    - $ref: Event.yaml
    - properties:
        topic:
          enum:
            - realestate/lead#update
        data:
          type: object
          properties:
            type:
              type: string
              enum:
                - UpdateAction
              example: UpdateAction
            object:
              $ref: ../schemas/Lead.yaml
              example:
                type: Lead
