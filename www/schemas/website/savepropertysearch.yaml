x-title: savepropertysearch
x-scope: realestate/website#
summary: website user (agent) saves a property search (object), on a website or mobile app (instrument)
payload:
  allOf:
  - $ref: website.event.yaml
  - properties:
      topic:
        description: realestate/website#savepropertysearch
        enum:
        - realestate/website#savepropertysearch
      data:
        properties:
          type:
            enum:
            - SaveAction
          object:
            $ref: ../PropertySearch.yaml
                