x-title: lead#reject
summary: a crm lead was accepted
x-scope: realestate
payload:
  allOf:
    - $ref: Event.yaml
    - properties:
        topic:
          enum:
            - realestate/lead#reject
        data:
          $ref: ../schemas/RejectAction.yaml
