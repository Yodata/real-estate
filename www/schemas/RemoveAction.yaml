allOf:
  - $ref: Action.yaml
  - type: object
    properties:
      type:
        type: string
        enum:
          - RemoveAction
      object:
        description: the item removed
      targetCollection:
        description: the collection from which the item is being removed
