title: Referral
type: object
description: describes a real estate referral from the referredBy (subProperty of agent) to the recipient
x-range: Referral
properties:
    type:
        $ref: type.yaml
        enum:
        - Referral
        description: Referral
    additionalProperty:
        type: object
        properties:
            isReferralYN:
                type: string
                description: Y if transaction was a referral
                enum:
                - Y
                - N
            bhhsInNetworkReferralYN:
                type: string
                description: Y if transaction was referred by an in network agent
                enum:
                - Y
                - N
    referredBy:
        type: object
        description: the referring entity (required properties shown)
        x-range:
        - RealEstateAgent
        - RealEstateOrganization
        properties:
            type:
                $ref: type.yaml
            id:
                $ref: id.yaml
