x-title: listing#offer
x-scope: realestate
summary: An offer made by the action.agent to purchase a listing
payload:
  allOf:
    - $ref: Event.yaml
    - properties:
        topic:
          enum:
            - realestate/listing#offer
        data:
          $ref: ../schemas/OfferAction.yaml
