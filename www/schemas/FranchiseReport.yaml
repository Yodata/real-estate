title: FranchiseReport
x-range: FranchiseReport
description: a franchisee business report
type: object
properties:
  type:
    type: string
    description: '"FranchiseReport"'
    enum:
    - FranchiseReport
  batchId:
    type: string
    description: BATCH-ID
    example: '1234'
  totalGci:
    $ref: MonetaryAmount.yaml
    description: TOTAL-GCI-IN-BATCH
  transaction:
    type: array
    description: transaction data
    x-range: TransactionSummary
    items:
      type: object
      example:
        type: TransactionSummary
      # $ref: TransactionSummary.yaml
  transactionParticipant:
    type: array
    description: transaction customers

  listingCount:
    type: object
    description: new listings and inventory count
    x-range: ListingCountSummary
    # $ref: ListingCountSummary.yaml
  associateProduction:
    type: array
    description: associate production summary
    x-range: AssociateProductionSummary
    items:
      $ref: SalesProductionReport.yaml