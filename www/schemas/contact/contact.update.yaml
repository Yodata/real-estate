x-title: update
x-scope: realestate/contact#
summary: a crm contact was updated
payload:
  allOf:
    - $ref: ../base.event.yaml
    - properties:
        topic:
          enum:
            - realestate/contact#update
        data:
          allOf:
            - $ref: ../UpdateAction.yaml
            - properties:
                object:
                  x-range: Contact
                  example:
                    type: Contact
                    description: see Objects > Contact for full schema details
                    identifier:
                      vendoraid: xxx
                    
                  
