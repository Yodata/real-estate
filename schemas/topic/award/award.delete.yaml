
messageId: award#delete
name: realestate/award#delete
title: award#delete
summary: award deleted
contentType: application/json
tags:
  - name: action|delete
  - name: topic|award
  - name: domain|realestate
headers:
  type: object
  properties:
    topic: { const: realestate/award#delete }
payload:
  type: object
  properties:
    data:
      type: object
      properties:
        type: { const: DeleteAction }
        identifier:
          $ref: ../../identifier.yaml
          description: the award identifier
          example:
            hsfAwardId: xxxx
        agent:
          $ref: ../../agent.yaml
          description: the award presenter
        object:
          $ref: AwardSeries.yaml
          description: the award being bestowed
        recipient:
          type: object
          properties:
            type: { type: string, enum: [RealEstateAgent, RealEstateOffice, RealEstateOrganization]}
            id:
              type: string
              format: uri
              description: profile URI of the award recipient
              example: https://user.example.com/profile/card#me
            name:
              type: string
              description: name of the award recipient
              example: Joann Agent
          example:
            type: RealEstateAgent
            id: https://user.example.com/profile/card#me
            name: Joann Agent