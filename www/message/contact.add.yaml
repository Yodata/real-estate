x-title: contact#add
x-scope: realestate
summary: a contact was added/created.
payload:
  allOf:
    - $ref: Event.yaml
    - properties:
        topic:
          enum:
            - realestate/contact#add
        data:
          allOf:
            - $ref: ../schemas/AddAction.yaml
            - properties:
                object:
                  x-kind: Contact
                  x-range: Contact
