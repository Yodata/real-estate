{{#if operation.summary}}
{{{operation.summary}}}

{{/if}}
{{#if operation.tags}}

### Tags
{{> tags tags=operation.tags}}
{{/if}}

{{#if operation.description}}

{{{operation.description}}}
{{else}}
Publish events by HTTP POST to your own pods `/publish/` endpoint including the topic, recipient and message body.]
{{/if}}

{{#if operation.headers}}

{{!-- {{> schema schema=operation.headers hideExample=true}} --}}

### Example
```http
POST /publish/ HTTP/1.1
Host: # { your pod host url }
x-api-key: # { your api key }
Content-Type: application/json

{
  "topic": "https://realestate/website#search",
  "recipient": # the publishing pod,
  "@context": # optional transformation context,
  "data": {
    "type": "SearchAction
  }

}
```
{{/if}}
