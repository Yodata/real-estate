x-title: lead#accept
summary: a lead (object) was accepted by the agent
x-scope: realestate
payload:
  allOf:
    - $ref: Event.yaml
    - properties:
        topic:
          enum:
            - realestate/lead#accept
        data:
          allOf:
            - $ref: ../schemas/AcceptAction.yaml
            - properties:
                object:
                  $ref: ../schemas/Lead.yaml
